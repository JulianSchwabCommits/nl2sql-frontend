services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000}
    restart: unless-stopped
    ports:
      - '5173:80'
    environment:
      - VITE_API_URL=${VITE_API_URL:-http://localhost:3000}

volumes: {}
